function input(char) {
  let vals = this.trie[c];

  for (let ch of vals) {
    if (ch == char) {
      this.trie = his.trie[ch];
    }
    let weight = this.trie[char];
    // based on the sorted values, get the weights
  }

  result = values;

  // If they are equal, start looping through each char, and then compare those
  // values
}
